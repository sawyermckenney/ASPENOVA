import type { NextConfig } from 'next';

/**
 * @fileoverview This file is intentionally left with a minimal configuration
 * as the primary Next.js configuration is now managed in `next.config.js`.
 * Having both `next.config.js` and `next.config.ts` can lead to `next.config.js`
 * taking precedence and `next.config.ts` (this file) being ignored.
 * All configurations have been consolidated into `next.config.js`.
 */

const nextConfig: NextConfig = {
  // All Next.js configurations have been moved to next.config.js
  // to ensure consistency and resolve potential conflicts.
  // This file is kept to satisfy TypeScript project structure if needed,
  // but it does not actively configure Next.js when next.config.js is present.
};

export default nextConfig;
